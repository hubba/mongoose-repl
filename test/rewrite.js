describe('Rewriting', () => {
    
    beforeEach(() => {
        this.rewrite = require('./../rewrite');
    });

    it('should rewrite correctly', () => {
        this.rewrite.eval(function () {

        });
    })

});